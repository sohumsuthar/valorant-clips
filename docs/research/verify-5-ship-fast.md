# verify:5-ship-fast

> Auto-generated by multi-agent workflow `wf_df45e754-ae5` on 2026-09-13.
> Agent label: `verify:5-ship-fast`.

{
  "claim": "origin/dev shares NO common ancestor with main (git merge-base returns nothing; dev root 6fcb757, main root 7958ea7) and is a separate Windows/blackbird build against drive letters K:/L:/C:/P: \u2014 so it must be cherry-picked as source files, never merged. The specific file this plan depends on, origin/dev:src/valclips/ai/audio.py (15,564 bytes, GUNFIRE_WINDOW=2.0 / GUNFIRE_MIN_PEAKS=3 / DEFAULT_PEAK_THRESHOLD_DB=-20.0), has never been benchmarked \u2014 the dev branch has no results file and no accuracy number anywhere. If its gunfire clustering is useless as a candidate generator, Stage A needs a real detector and Weekend 2 becomes two weekends.",
  "verdict": "PARTIALLY_TRUE",
  "evidence": "ALL GIT/FILE FACTS CONFIRMED EXACTLY.\n- `git merge-base origin/main origin/dev` \u2192 no output, exit 1. `git merge-base --all origin/main origin/dev` \u2192 no output, exit 1. `git merge-base origin/main origin/master` \u2192 exit 1 too.\n- `git rev-list --max-parents=0`: origin/dev \u2192 6fcb757065c1b7b047da571457d39fb893ac8023; origin/main \u2192 7958ea7b308a3618d70a6acf70bfed9ab80b1a93. (origin/master shares dev's root 6fcb757; `git merge-base origin/master origin/dev` \u2192 582d3cc.)\n- `git ls-tree -r -l origin/dev -- src/valclips/ai/` \u2192 audio.py blob 5576b21082b82d6b3422e4391fed47ac2c05d176, **15564** bytes; `git cat-file -s 5576b21` \u2192 15564; 485 lines. Exact.\n- Constants verified in the blob: audio.py:39 `GUNFIRE_WINDOW = 2.0`, :40 `GUNFIRE_MIN_PEAKS = 3`, :35 `DEFAULT_PEAK_THRESHOLD_DB = -20.0`. Exact.\n- NO BENCHMARK ANYWHERE \u2014 confirmed exhaustively. Grepped every tracked non-image file on origin/dev for accuracy|precision|recall|f1|benchmark|ground.?truth|validated: only 5 marketing-copy hits (\"best accuracy\" in README.md:57, cli.py:430, ollama_analyzer.py:6/:417). dev's `tests/` holds only test_db.py and test_parser.py \u2014 zero tests import valclips.ai. Working tree: no clips.db, no results/eval/csv/json artifacts outside .venv. `gh api repos/sohumsuthar/valorant-clips/issues` \u2192 empty; releases \u2192 only tag `clips-v1`. `git ls-remote origin` confirms our refs are current (dev=2802db6, main=ed7f691).\n- THE DEPENDENCY IS REAL: main has zero audio analysis. `git show origin/main:src/valclips/ai/ffmpeg_analyzer.py` (7,763 B) defines only _get_scene_score/_get_bitrate_variance/_folder_bonus/_tag_bonus/_duration_signal; local_analyzer.py is pure OpenCV HUD pixels. No silencedetect/volumedetect/gunfire token exists on main. dev's audio.py is the only audio module in the repo.\n\nWHERE THE CLAIM IS WRONG (characterization):\n1. \"a separate Windows/blackbird build against drive letters K:/L:/C:/P:\" \u2014 WRONG. `git show origin/dev:src/valclips/config.py` lines 13-24 contain the IDENTICAL Linux/GVFS block as main: `GVFS_BASE = Path(f\"/run/user/{_uid}/gvfs\")` + `smb-share:server=chaos.local,share={prometheus,kronos,atlas}`. The drive letters are ADDITIVE, gated at config.py:77 `if sys.platform == \"win32\"`, and there are FIVE, not four: S: (config.py:79 `Path(\"S:/NVIDIA\")` \u2014 the actual ShadowPlay dir, omitted from the claim), K: (:95), L: (:112), P: (:121), C: (:136). dev is a cross-platform superset of main's config, not a separate Windows build.\n2. \"blackbird\" \u2014 UNVERIFIED, appears nowhere in the repo. All 13 non-root dev commits are authored `sohumsuthar <37936322+sohumsuthar@users.noreply.github.com>`; nothing identifies a machine.\n3. The proposal's supporting line (.private/research/propose-ship-in-a-weekend.md:50) calls them \"two unrelated projects.\" REFUTED: **16 blobs are byte-identical** between main and dev, including pyproject.toml, tests/test_db.py, tests/test_parser.py, extractor.py, thumbnails.py, and 6 of 9 ai/ modules (base.py, stub.py, keyframes.py, highlights.py, claude_analyzer.py, gemini_analyzer.py). dev root 6fcb757 = 2026-02-23 19:34:16 -0500; main root 7958ea7 = 2026-02-24 00:38:24 -0500 \u2014 same author, same day, **5h04m later**. main is a re-`git init` of the same codebase. The missing merge-base is a plumbing artifact of that re-init, not evidence of unrelated work.\n\nTHE CONDITIONAL IS REFUTED AS FAR TOO WEAK \u2014 I MEASURED IT.\nI extracted origin/dev:src/valclips/ai/audio.py verbatim (sole edit: `from ..config import FFPROBE_TIMEOUT` \u2192 `FFPROBE_TIMEOUT = 30` to import standalone) and ran `detect_gunfire_segments` against 7 synthetic 20-30 s H.264+AAC clips with exact ground truth (6 or 4 \"gunfights\", 5-8 impulsive 50-60 ms shots each). Scratchpad: /private/tmp/claude-501/-Users-sohum-Documents-GitHub/36b90c59-6e28-4a77-baa7-678b0e3b60cc/scratchpad/gftest/\nRESULT: **0 gunfire segments returned in 7 of 7 conditions. Recall 0/6 and 0/4 in every single one** \u2014 including the maximally favorable case (clean shots over digital silence).\nTwo independent, reproducible structural defects:\n(a) audio.py:189 `if gap_end - gap_start > 0.05:` \u2014 a loud segment between two silences is discarded unless it exceeds 50 ms. A gunshot transient IS ~50 ms. In the 320 ms-spacing (Sheriff-rate) test, ffmpeg silencedetect correctly returned **21 silence intervals** but `_invert_silence` produced exactly **1 loud segment** \u2014 every shot was filtered out by that guard.\n(b) audio.py:398-423 \u2014 the cluster test needs \u22653 SEPARATE loud-segment midpoints inside GUNFIRE_WINDOW=2.0 s, but `detect_silence(noise_db=-25.0, min_duration=0.15)` (audio.py:390-393) cannot emit a silence interval for a gap under 150 ms, so a burst of automatic fire collapses to ONE segment with ONE midpoint. Measured on the silent-background case: loud segments at 2.000-2.587, 6.500-7.087, 10.996-11.587, 15.498-16.089, 19.999-20.588, 24.500-25.088 \u2192 midpoints [2.29, 6.79, 11.29, 15.79, 20.29, 24.79] \u2192 **max bursts in any 2.0 s window = 1, need \u22653**. Riot's own wiki gives Phantom **11 rounds/sec (90.9 ms)** and Vandal **9.75 rounds/sec (102.6 ms)** \u2014 both below the 150 ms floor, so automatic fire can never be resolved into separate segments. Satisfying \u22653-in-2.0 s would require three distinct gunfights inside two seconds.\n(c) Regime failure: with continuous background above the -25 dB gate (my -20 dBFS bed, and a pure-music clip) `detect_silence` returns 0 intervals \u2192 `_invert_silence` returns one whole-clip segment of duration \u22651.5 s \u2192 the `seg.duration < 1.5` filter empties burst_times \u2192 early `return []`. Real ShadowPlay captures desktop audio + game music + comms; this is the normal case.\nPARTIAL SALVAGE: `detect_audio_peaks` (the other half of the proposal's \"peak density + gunfire clustering\") is not dead but is regime-fragile. Silent/-35 dB background: 7 peaks at [2.29, 6.79, 11.29, 15.79, 20.29, 24.79] vs ground truth [2.0, 6.5, 11.0, 15.5, 20.0, 24.5] \u2014 6/6 within 1 s at a constant +0.29 s offset. Realistic -20 dB continuous background: 6 peaks at [5.13, 11.96, 14.22, 18.05, 25.42, 30.52] \u2014 **0/6 aligned**. `get_audio_intensity_score` gave score 0.3369 (30 shots) vs 0.07 (pure music, no shots) \u2014 a 4.8\u00d7 separation, but on the easiest possible discrimination.\n\nSources: [Phantom - VALORANT Wiki](https://wiki.playvalorant.com/en-us/Phantom), [Vandal - VALORANT Wiki](https://wiki.playvalorant.com/en-us/Vandal), [Vandal - Liquipedia](https://liquipedia.net/valorant/Vandal)",
  "correction": "Three corrections. (1) The merge-base facts are exactly right and the operational conclusion \"cherry-pick source files, never merge\" stands \u2014 but the stated REASON is wrong. main and dev are not unrelated projects and dev is not a Windows-only build: 16 blobs are byte-identical, dev's config.py keeps main's full Linux/GVFS chaos.local block verbatim and merely ADDS five Windows drives (S:/K:/L:/P:/C:, not K:/L:/C:/P:) behind `if sys.platform == \"win32\"` at config.py:77, and main's root commit is a re-init of dev's codebase 5h04m later on the same day by the same author. \"blackbird\" is unsupported inference \u2014 the string appears nowhere in the repo. (2) Drop the hedge. `detect_gunfire_segments` is not \"unbenchmarked, and might be useless\" \u2014 it is now benchmarked and it returns ZERO segments on 7/7 synthetic clips with ground truth, including the maximally favorable silent-background case. Root causes are two hard defects: audio.py:189's `> 0.05` guard deletes every ~50 ms gunshot transient, and the \u22653-midpoints-in-2.0 s test at audio.py:398-423 is unsatisfiable because silencedetect's `min_duration=0.15` floor exceeds the 91-103 ms inter-shot interval of every automatic weapon in the game, so one gunfight yields one midpoint. (3) Therefore the plan's Stage A must budget a REWRITE, not the \"port + merge two ffmpeg calls into one filter chain + add -discard:v all\" described at propose-ship-in-a-weekend.md:82 and :227 \u2014 those are I/O optimizations that touch neither defect. The claim's own contingency (\"Weekend 2 becomes two weekends\") has already fired. Cheapest real fix, in order: (a) delete the `> 0.05` guard and drop silencedetect's min_duration to ~0.03 s, or better (b) abandon silencedetect entirely for `ffmpeg -af astats=metadata=1:reset=1` frame-level RMS (or an `aresample`+numpy envelope) with onset picking at ~10 ms resolution, keeping `detect_audio_peaks`' surviving behavior only as a sanity baseline; (c) the docs' own recommendation of YAMNet frozen embeddings (AudioSet class `Gunshot, gunfire`, 0.96 s window / 0.48 s hop, cited at .private/research/propose-research-ambitious.md:109) is the credible Stage A, and Sohum already has prior art in yamnet-beat-insight/audiosense-stream.",
  "impact_if_false": "Low impact from the git half, high impact from the detector half. The \"never merge, cherry-pick files\" instruction is operationally correct regardless of the bad rationale \u2014 `git merge origin/dev` would need `--allow-unrelated-histories` and would produce parallel implementations of every module. Nothing in the plan breaks there; only the dossier's credibility does, and the \"two unrelated projects\" framing risks discarding dev's other genuinely-new modules (cv_analyzer.py 13,680 B, motion.py 9,264 B, ollama_analyzer.py 20,742 B) as foreign code when they are the same project's later work. The detector half is load-bearing and already broken: Stage A of Weekend 2 (propose-ship-in-a-weekend.md:137-145, :227) feeds the entire cascade \u2014 audio candidates \u2192 CV kill-banner confirm \u2192 events table \u2192 /moments UI \u2192 Final Cut export. With `detect_gunfire_segments` at 0% recall, Stage A emits zero candidates, Stage B never runs, the events table stays empty, and Weekends 2 and 3 both produce nothing. The plan's own abort criterion at :236 (\"audio\u2192banner cascade scores below 80% precision or 70% recall\") would trip on the first run against the Stage C fixture set. The silver lining is that the Stage C hand-labeling step (:227, \"2 hours, non-negotiable\") is what surfaces this \u2014 so the right move is to reorder: build moments/eval.py and the 20-clip labels.jsonl BEFORE porting audio.py, not after, and expect to write a real onset detector rather than port a 15,564-byte file that has never produced a single true positive."
}
