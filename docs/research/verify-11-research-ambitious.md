# verify:11-research-ambitious

> Auto-generated by multi-agent workflow `wf_df45e754-ae5` on 2026-09-13.
> Agent label: `verify:11-research-ambitious`.

{
  "claim": "2 FPS SAMPLING IS FREE. ShadowPlay writes an IDR keyframe every 0.50 s uniformly, so `ffmpeg -discard nokey` yields exactly 2 fps at 78x realtime with a 25x CPU saving. MEASURED on exactly ONE file (161 keyframes in 80.1 s, intervals uniformly 0.50 s). If GOP structure varies across the 2023-2026 encoder-settings span, the effective HUD sample rate varies with it \u2014 and the 4-second kill-banner visibility window sets the recall floor. The moov stss census verifies this archive-wide for free in Phase 1.",
  "verdict": "PARTIALLY_TRUE",
  "evidence": "I extended the sample from n=1 to n=18 using real clips found on this Mac (the NAS is offline). Method: `ffprobe -v error -select_streams v:0 -show_packets -show_entries packet=pts_time,flags -of csv=p=0` (demux-only, K-flag = MP4 `stss` sync sample), then interval stats. Script: /private/tmp/claude-501/-Users-sohum-Documents-GitHub/36b90c59-6e28-4a77-baa7-678b0e3b60cc/scratchpad/sig.py\n\n(1) CORE PREMISE CONFIRMED, AND FAR BETTER SUPPORTED THAN THE CLAIM'S OWN HEDGE. All 12 files identifiable as un-re-encoded NVIDIA-muxed originals show mean interval 0.500 s, across the FULL 2023-2026 span and BOTH encoder generations:\n - GFE/MainConcept muxer (handler_name \"Mainconcept Video Media Handler\", encoder \"AVC Coding\", 1 audio track): VALORANT_replay_2023.01.04-21.51.mp4 (720p, 10 Mbps) mean 0.500 min 0.500 max 0.500 sd 0.0000; Valorant 2024.06.03 DVR_1 (41 Mbps) 0.500/0.500/0.500; Valorant 2025.02.28 DVR_1 (39 Mbps) 0.500; Valorant 2025.04.29 (33 Mbps) 0.500 sd 0.0000.\n - NVIDIA App muxer (handler_name \"VideoHandle\"/\"SoundHandle\", tracks named \"System sounds\"/\"Microphone\"): 2024.12.17-20.27 (kfps 2.005), 2025.02.28 (2.000), 2025.04.03 (2.009), 2026.08.10 (2.002), 2026.08.12 (2.007), 2026.08.17-18.17 (1.999), 2026.08.17-19.51 (2.001), 2026.08.17-20.05 (2.010).\n Worst sd across all 12 = 0.0032 s; worst single interval = 0.541 s. Spans 720p/1080p, 10-96 Mbps, 30/59.94/60 fps, Main@4.2 and High@5.0, 1 and 2 audio tracks. The claim's stated risk (\"if GOP structure varies across the 2023-2026 encoder-settings span\") is resolved IN ITS FAVOR: it does not.\n\n(2) \"25x CPU SAVING\" IS REFUTED \u2014 MEASURED 11.2x. On ~/Documents/Valorant 2026.08.17 - 20.05.57.04.DVR.mp4 (763,796,186 B, 80.10 s, 4,802 video packets, 161 K-flagged), warm cache, ffmpeg 8.1:\n - `-threads 1 -discard nokey`: real 5.26 / user 5.19 s. `-threads 1` full decode: real 59.25 / user 58.34 s \u2192 11.24x.\n - Default threads: `-discard nokey` user 6.46 s vs full decode user 73.21 s \u2192 11.33x. Wall 1.00-1.03 s vs 12.07 s \u2192 12.1x.\n The two thread configurations agree at ~11.2-11.3x. The \"25x\" is almost certainly the FRAME ratio, not a CPU ratio: 4,802 total frames / 198 emitted = 24.25 \u2248 25. IDR frames are intra-coded and cost ~2.2x the average frame, so skipping 96% of frames buys only 11x of CPU.\n (The companion \"-skip_frame nokey gives only 2.4x\" IS correct: measured real 5.00 s vs 12.07 s full = 2.41x.)\n\n(3) \"EXACTLY 2 FPS\" IS FALSE AS OUTPUT \u2014 MEASURED 2.473 fps. `ffmpeg -discard nokey -i F -map 0:v:0 -vf showinfo -f null -` emits 198 frames, not 161, over 0.0 \u2192 80.0658 s = 2.473 fps. Interval histogram: 156 intervals at ~0.5004 s plus 38 intervals at 0.0166-0.0168 s (exactly one 59.94 fps frame period). ffmpeg emits ~37 extra frames adjacent to IDRs. Max gap 0.5039 s, so TEMPORAL COVERAGE is genuinely uniform 2 Hz and recall is unaffected \u2014 but the output frame count does not equal the stss count, and `t_i = i/2.0` is wrong.\n\n(4) \"78x REALTIME\" CONFIRMED ONLY AS A WARM-CACHE, LOCAL-NVMe, ~6.5-CORE NUMBER. 1.00-1.03 s wall for 80.10 s = 78-80x; ffmpeg's own stats line reported speed=82.8-84.3x. Single-threaded it is 15.2x (5.26 s). Because `-discard nokey` saves ZERO I/O (the proposal concedes this), sustaining 78x requires 763,796,186 B / 1.027 s = 744 MB/s = 5.95 Gbps from the NAS. At 1 GbE (~118 MB/s effective) it collapses to 6.47 s/clip = 12.4x realtime, a 6.3x shortfall. At 10 GbE it is ~115x, i.e. CPU-bound again at ~78x. mahadev\u2194truenas-scale link speed is UNVERIFIED (NAS offline all session).\n\n(5) THE REAL VARIANCE RISK IS PROVENANCE, NOT ENCODER DRIFT \u2014 AND THE CLAIM MISSES IT. 6 of 18 probeable Valorant-named files on this Mac are re-encoded derivatives, and those DO deviate: \"Valorant 2024.12.17 - 22.42.44.09.DVR.mp4\" (~/Documents/Adobe/Premiere Pro/22.0/) mean 1.995 s \u2192 0.505 kfps; \"Valorant 2024.12.17 - 20.27.17.04.DVR_1.mp4\" (1080x1920 PORTRAIT crop) 1.989 s \u2192 0.522 kfps; \"Valorant 2024.06.03 - 20.39.03.03.DVR.mp4\" scene-cut adaptive, min 0.017 / max 1.000, sd 0.313 \u2192 1.240 kfps; \"Valorant 2025.01.06 ....MP4.mp4\" (iMessage, handler \"Core Media Video\", 720p 2 Mbps) \u2192 1.059 kfps; \"vlc-record-2025-07-25-...MOV.mp4\" (604x1076) max gap 1.468 s \u2192 1.856 kfps. Critically, handler_name \"Mainconcept\" does NOT discriminate ShadowPlay from Adobe Media Encoder \u2014 Adobe licenses MainConcept, so the 2.0 s-GOP Premiere export carries a byte-identical encoder signature to genuine GFE originals. He demonstrably edits: ~/Movies/val.fcpbundle/8-14-26/ and four .prproj files in ~/Documents/Adobe/Premiere Pro/22.0/.\n\n(6) moov/stss SUB-CLAIM CONFIRMED, BUT THE 256 KB TAIL READ IS UNDER-SPECIFIED. Box walk (scratchpad/moov.py): 2026.08.17-20.05 top level = ftyp(24 B) / mdat(763,681,185 B) / moov(114,977 B) \u2014 moov at 99.98% of file, 0.0151% of size, exactly matching the proposal's \"114,977 bytes = 0.015%\". stss exists (161 of 4,802 packets K-flagged; if stss were absent all 4,802 would be K). BUT moov \u2248 24 B/frame, so 256 KB \u2248 10,900 frames \u2248 182 s at 59.94 fps \u2014 any clip over ~3 minutes overruns the window and the census silently fails. Also moov is not always last: 2025.02.28 has a trailing uuid box (4,721 B), so 162,086 B lie between moov start and EOF. Correct fix is cheaper AND exact: pread 16 B at offset 0 (ftyp size) and 16 B at offset 24 (mdat size) \u2192 moov offset = 24 + mdat_size.\n\n(7) THE 4-SECOND KILL-BANNER WINDOW IS UNVERIFIED. WebSearch returned no authoritative duration; valorant.fandom.com/wiki/Kill_Banners and wiki.playvalorant.com/en-us/Kill_Banners describe the banner but state no timing, and the kill feed is user-configurable (size/position/style/filter), so the window may not be a constant. Note the wiki places the kill banner at the BOTTOM of the screen, matching the repo (src/valclips/ai/local_analyzer.py:19 \"Kill banner: bottom-right area with yellow marker (HSV H=26-32, S=106-135)\") rather than the proposal's \"under the crosshair\".\n\n(8) REPO CROSS-REFERENCE CONFIRMED. src/valclips/ai/local_analyzer.py:390 `def __init__(self, num_frames: int = 8)`, :391-392 set self.num_frames/self.max_frames, :405 `for frame_path in keyframes[:self.num_frames]` \u2014 the 8-frames-per-clip defect the claim cites is real and exactly as described.\n\nThree 2023 files (~/Library/CloudStorage/OneDrive-TheOhioStateUniversity/val/VALORANT_replay_2023.11.*.mp4, 755 MB each) are un-hydrated OneDrive placeholders \u2014 ffprobe returned \"Operation timed out\". The 2023.11 era is therefore UNTESTED; only 2023.01 was measurable.",
  "correction": "Corrected claim: ShadowPlay does write an IDR every 0.500 s, and this now holds on 12/12 identifiable originals spanning 2023.01-2026.08 and both NVIDIA muxer generations (GFE/MainConcept and NVIDIA App/VideoHandle) \u2014 the n=1 hedge should be retired. But: (a) `ffmpeg -discard nokey` emits 2.473 fps (198 frames), not 2.000 (161) \u2014 ~37 spurious frames land one frame-period from an IDR; temporal coverage is still uniform 2 Hz (max gap 0.5039 s), so dedupe on |dt| < 0.1 s and never compute timestamps as i/2.0. (b) The CPU saving is 11.2x (single-thread user 5.19 s vs 58.34 s), not 25x; \"25x\" is the 4,802/198 frame ratio misread as a CPU ratio. (c) 78x realtime is a warm-cache local-NVMe multicore ceiling; since -discard nokey saves zero I/O it needs 744 MB/s = 5.95 Gbps sustained, so on 1 GbE the pipeline runs at ~12.4x realtime. Budget Phase 3 from bytes/s, not from 78x. (d) The variance risk is misattributed: the encoder is stable, the ARCHIVE is not. 6/18 local Valorant files are Premiere/Final Cut/VLC/iMessage derivatives sampling at 0.505-1.86 kfps, and MainConcept handler tags cannot distinguish an Adobe export from a GFE original. Gate on a measured per-file kfps from stss, not on file naming or handler_name. (e) Replace the \"one pread at size-256 KB\" with two 16-byte preads at offsets 0 and 24 to locate moov exactly \u2014 256 KB truncates moov for any clip over ~3 minutes and moov is not always the last box. (f) The 4 s banner window is unverified and user-configurable; measure it on his own footage before using it to set a recall floor.",
  "impact_if_false": "Mostly bounded, with three real bugs. The 11.2x-vs-25x CPU error does NOT break the plan: Phase 3 is I/O-bound either way (the proposal itself budgets 3-5 h of reads for 1-2 TB \u2248 79 MB/s \u2248 1 GbE), and even a 2.2x-slower CPU path still outruns the network, so S3's \"~4 h runtime\" survives. The genuinely damaging items are: (1) the 2.473-vs-2.000 fps mismatch \u2014 any Phase 3 code that indexes HUD samples as t = i/2.0, or asserts len(frames) == stss_count, produces timestamps drifting up to 23% and silently corrupts every cross-modal offset measurement in Phase 4, which is the paper's methodological crux and whose GATE 3 (offset sigma < 300 ms) would then be measuring an artifact of the frame indexer rather than the game; (2) provenance heterogeneity \u2014 if the 10 TB archive mixes Premiere/portrait exports with originals at the ~33% rate seen locally, that fraction samples at 0.505-1.24 kfps, giving ~2 banner samples per kill instead of ~8, so kill recall degrades non-uniformly and, because the derivatives are the EDITED highlight clips, the loss is concentrated exactly on the most kill-dense footage, biasing the training set; (3) the 256 KB tail read silently fails on clips over ~3 minutes, i.e. full-match manual recordings, so GATE 1 would report a clean census while having skipped the largest files. All three are cheap to fix before Phase 1 and none invalidate the decision to sample at keyframes."
}
