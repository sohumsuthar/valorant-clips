# verify:3-ship-fast

> Auto-generated by multi-agent workflow `wf_df45e754-ae5` on 2026-09-13.
> Agent label: `verify:3-ship-fast`.

{
  "claim": "Audio extraction with `-discard:v all` genuinely seeks rather than reading linearly: audio packets are 0.252% of file bytes, and an 80.1 s clip's audio extracts in 0.16 s \u2014 faster than a bare sequential read of the same file at 3.55 GB/s \u2014 with sha1-identical output. If the actual SMB mount forces 1 MB readahead this becomes 11.4% (1.14 TB, 2.8 h instead of 25 min); if the CIFS client somehow reads linearly it becomes 10 TB and the whole cascade collapses to a 24.8-hour job. Verify with rsize=131072 and a byte counter before committing to Weekend 2.",
  "verdict": "PARTIALLY_TRUE",
  "evidence": "METHOD. I built a DYLD_INSERT_LIBRARIES interposer on read()/lseek() (source: /private/tmp/claude-501/-Users-sohum-Documents-GitHub/36b90c59-6e28-4a77-baa7-678b0e3b60cc/scratchpad/interpose2.c, built as libiolog.dylib; /opt/homebrew/bin/ffmpeg is adhoc-signed, flags=0x2, no hardened runtime, so interposition works). It logs every (offset, size) read on the input fd. Analyzer: scratchpad/blocks2.py. ffmpeg 8.1 (libavcodec 62.28.100). Reference clip is the one the proposal actually used: ~/Documents/Valorant 2026.08.17 - 20.05.57.04.DVR.mp4 \u2014 duration 80.099333 s (the \"80.1 s\"), 763,796,186 B, h264 1920x1080@60 75,889,524 bps, TWO aac tracks @ 191,999 + 192,002 bps. 191999/(763796186*8/80.0993) = 0.2517% \u2014 so the claim's \"0.252%\" is the single-track *bitrate ratio* of this one clip, not bytes read and not the archive.\n\n1. THE MECHANISM IS REAL. Forcing video packets to be read (`-discard:v none`, or `-map 0:v:0 -c:v copy -f null -`) reads 763,872,815 B = 100.010% of the file. With video discarded it reads 10,540,669 B. So libavformat's mov demuxer does seek past video samples; it does not read linearly. CONFIRMED.\n\n2. `-discard:v all` IS A NO-OP AND CAUSES NONE OF THAT. FFmpeg master fftools/ffmpeg_demux.c, ist_add(): `st->discard = AVDISCARD_ALL;` is the *initial* value for every input stream; a user `-discard` only overrides it. Measured, running the proposal's exact Stage A command from propose-ship-in-a-weekend.md:139-142 with and without the flag: raw=10,540,669 B both times, and identical at every block granularity (@32K 13.14 MB, @128K 21.63 MB, @1024K 96.47 MB), and the decoded PCM sha1 is identical (8d9dfc97c6f8344d961d49e607efd6fbd44f0a0f both). `-c:a copy` variants: sha1 f149aee7ae560f12546aa641f913e01985320202 with and without. The prescribed \"fix\" at :82 (\"add `-discard:v all` so the demuxer seeks instead of reading linearly\") buys exactly zero bytes.\n\n3. 0.252% IS WRONG BY 5.5x ON ITS OWN REFERENCE CLIP AND 7.7x ARCHIVE-WIDE. The proposal's own command maps BOTH audio tracks (`-map 0:a:0 ... -map 0:a:1?`), so the bitrate ratio is already 0.503%, not 0.252%. Actual bytes read on the reference clip: 10,540,669 = 1.380% of the file. Breakdown from the I/O log: 147,777 B header+moov; 5,150,044 B read CONTIGUOUSLY from offset 40 (this is the default `probesize 5000000` in avformat_find_stream_info \u2014 it reads video packets linearly and `-discard` does not stop it); then 83 seek+32768 B reads, ~12 MB apart, for the audio chunks. The 5.15 MB probe is a fixed per-clip cost 2.7x larger than the entire claimed budget.\nMeasured across all 11 real ShadowPlay clips on this Mac (~/Documents/*.DVR.mp4 + ~/Downloads/Valorant*.mp4 + VALORANT_replay_2023.01.04-21.51.mp4), 5.26 GB total, exact Stage A command:\n  raw bytes read: byte-weighted 1.941%; per-clip range 1.153% \u2013 15.580%. 10 TB -> 194 GB, 0.48 h @112 MB/s.\nPer-clip raw %: 1.16, 1.30, 1.37, 1.38, 1.92, 1.97, 2.04, 4.02, 4.52, 4.96, 15.62. Not one clip is near 0.252%; the best is 4.6x above it.\n\n4. READAHEAD AMPLIFICATION IS WORSE THAN STATED, AND THE DEFAULT IS 4 MB NOT 1 MB. Distinct-block model over the same 11 clips (the correct model for a page-cache-backed network FS):\n  @32 KB  : 2.767% byte-weighted (min 1.424%, max 30.182%) -> 277 GB, 0.69 h\n  @128 KB : 5.196% byte-weighted (min 2.308%, max 70.952%) -> 520 GB, 1.29 h\n  @1 MB   : 21.931% byte-weighted (min 10.284%, max 101.298%) -> 2.19 TB, 5.44 h\n  @4 MB   : 57.128% byte-weighted (min 35.527%, max 104.791%) -> 5.71 TB, 14.17 h\nman7.org/linux/man-pages/man8/mount.cifs.8.html, rsize: \"Maximum amount of data that the kernel will request in a read request in bytes... Default requested during mount is 4MB.\" So the *default* case is 14.2 h, not 25 min \u2014 and the 2023-era 720p/10.5 Mbps clip reads 101.3% of itself at 1 MB and 104.8% at 4 MB, i.e. the seek strategy already degenerates to a full linear read for low-bitrate clips, which the 2023\u20132026 archive demonstrably contains.\n\n5. THE TIMING CLAIM DOES NOT REPRODUCE. The proposal's exact Stage A command (two libopus encodes) on the 80.1 s clip: real 1.01 s, 1.88 s, 1.57 s (5 runs of the 1-track variant: 1.21/1.30/1.27/1.23/1.21 s). Only `-c:a copy -f adts /dev/null` hits 0.09\u20130.10 s. Meanwhile a bare sequential read of the same 763,796,186 B file on this machine: 0.226 s page-cached (3.38 GB/s \u2014 this is where \"3.55 GB/s / 0.215 s\" comes from), 0.046 s with F_NOCACHE (16.6 GB/s), 0.061 s via dd bs=1m (11.9 GB/s). So the proposal's own command is 4\u201340x SLOWER than the bare sequential read it claims to beat. The comparison is also a category error: it benchmarks against local NVMe bandwidth, while the real constraint on SMB is 83\u2013267 seek round-trips per clip at ~110 MB/s link speed.\nAlso: \"~500x realtime per core\" holds only for `-c:a copy` (80.1 s / 0.09 s = 890x); with the proposed libopus encode it is 80.1/1.25 = 64x realtime.\n\n6. \"sha1-identical output\" IS ONLY TRUE FOR `-c:a copy`. The Ogg/Opus outputs the proposal actually produces carry a randomized Ogg serial number: o1.opus d6eab14b... vs o3.opus c5a08210... vs o4.opus fd66684d... \u2014 all different files, identical decoded PCM. Use `-bitexact` or compare decoded PCM.\n\n7. THE PRESCRIBED MITIGATION MAY NOT EXIST ON THE ACTUAL MOUNT. `rsize=` is a `mount.cifs` (Linux kernel CIFS) option. The repo's documented and coded access path is GVFS: src/valclips/config.py:16-22 builds `/run/user/{uid}/gvfs/smb-share:server=chaos.local,share={prometheus,kronos,atlas}`; CLAUDE.md:29 and README.md:5 say the same. gvfsd-smb is libsmbclient in userspace over FUSE and exposes no rsize/readahead knob \u2014 linux-cifs list, https://www.spinics.net/lists/linux-cifs/msg12086.html: \"Both projects use the libsmbclient API (smbc_read/write), currently with buffer sizes of about 64kB\" and \"Typical throughput of gvfs-smb and kioclient over Gbit networks are only 5-30MB/s whereas mount/cifs and smbclient achieve close to wirespeed, ie > 100MB/s\" (see also https://gitlab.gnome.org/GNOME/gvfs/-/work_items/292). At 5\u201330 MB/s the 112 MB/s figure underpinning every budget in the proposal is 4\u201322x optimistic unless the pipeline is re-pointed at a kernel mount.cifs mount.\n\n8. UNVERIFIED / NOT TESTABLE HERE: no SMB mount exists on this Mac (`mount | grep -i smb` empty, /Volumes has no shares), `chaos.local` does not resolve, and truenas-scale shows \"offline, tx 936 rx 0\" in `tailscale status`. Every number above is measured on APFS/NVMe (/dev/disk3s5). The readahead figures in (4) are a distinct-block model, not a measured CIFS trace. The archive-wide byte-weighting uses 11 local clips (5.26 GB), not the real 10 TB mix; if the real archive is more heavily weighted to pre-2024 low-bitrate clips than this sample, every percentage above goes UP.\n\n9. A REAL WIN THE PROPOSAL MISSES: `-probesize 32768 -analyzeduration 0` kills the 5.15 MB linear probe. Reference clip drops from 10,540,669 B (1.380%) to 5,598,418 B (0.733%) \u2014 47% less I/O \u2014 and both audio streams are still correctly detected (`ffprobe -probesize 200000 -analyzeduration 0` returns streams 0 h264, 1 aac ch2, 2 aac ch2). Sweep: probesize 32768/200000 -> 0.733%, 1e6 -> 0.841%, 5e6 (default) -> 1.380%, 2e7 -> 1.875%.",
  "correction": "The seek mechanism is real; the attribution and every number are wrong. (a) `-discard:v all` is redundant \u2014 fftools/ffmpeg_demux.c already initializes `st->discard = AVDISCARD_ALL` for all input streams; measured byte-for-byte identical I/O with and without it. Drop it or keep it, it changes nothing. (b) Replace \"0.252%\" with 1.94% byte-weighted (range 1.15%\u201315.6% per clip) for the proposal's own two-track command \u2014 that is bytes actually read, and it includes a fixed ~5.15 MB per-clip `probesize` linear video read that `-discard` does not suppress. Add `-probesize 32768 -analyzeduration 0` and it falls to ~0.73%\u20131.3%. (c) Replace \"0.16 s\" with ~1.0\u20131.9 s for the exact Stage A command (libopus encode dominates); 0.09 s is achievable only with `-c:a copy`. (d) Delete the \"faster than a bare sequential read at 3.55 GB/s\" line \u2014 measured sequential read of that same file is 0.046\u20130.226 s (3.4\u201316.6 GB/s), i.e. faster than the extraction, and it is the wrong yardstick anyway. (e) \"sha1-identical\" holds only for `-c:a copy`; Ogg/Opus output has a random serial number \u2014 compare decoded PCM or pass `-bitexact`. (f) The readahead ladder is: 32 KB 2.77% (277 GB, 0.7 h) / 128 KB 5.20% (520 GB, 1.3 h) / 1 MB 21.9% (2.19 TB, 5.4 h) / 4 MB 57.1% (5.71 TB, 14.2 h) at 112 MB/s \u2014 and mount.cifs's documented default rsize is 4 MB, not 1 MB, so the *unmitigated* case is 14.2 h, not 25 min, which shrinks the cascade's advantage over the 24.8 h naive pass from 13x to 1.75x. (g) `rsize=131072` is a mount.cifs option and does not exist on the GVFS path the repo actually uses (config.py:16-22); gvfs-smb reads through libsmbclient in ~64 KB buffers at 5\u201330 MB/s on 1 GbE. The real prerequisite is not \"set rsize\" \u2014 it is \"stop using GVFS and mount with mount.cifs on the Spark, then set rsize=131072 and rasize/read_ahead_kb to 128 KB\". (h) Low-bitrate 2023-era clips (10\u201312 Mbps 720p) degenerate to reading 66\u2013101% of the file at 1 MB granularity; they need a different strategy (or accept a full read) regardless of mount tuning.",
  "impact_if_false": "Stage A's headline \"166 GB / ~25 min\" is off by 3\u201334x depending on mount granularity. The realistic band is 0.7 h (128 KB, mount.cifs, probesize trimmed) to 14.2 h (mount.cifs defaults, rsize=4 MB) to effectively unbounded on GVFS at 5\u201330 MB/s. The \"measured budget for the entire 10 TB: ~110 minutes\" at propose-ship-in-a-weekend.md:17 is only reachable if (1) the archive is re-mounted with mount.cifs \u2014 not GVFS \u2014 on the Spark, (2) rsize/readahead is explicitly pinned to 128 KB, and (3) `-probesize` is cut. Miss any one and Weekend 2's Stage A alone exceeds the whole 110-minute budget; miss all three and the 13x advantage over the 24.8 h naive pass falls to 1.75x, which does not justify building the cascade at all. Separately, anyone who \"verifies\" this by adding `-discard:v all` and watching the clock will see no change and may wrongly conclude the mount is broken \u2014 the flag is a placebo. The one action item that survives intact is the last sentence of the claim: instrument a byte counter on the real mount before committing, because none of this is testable from this Mac (no SMB mount, chaos.local unresolvable, truenas-scale offline in `tailscale status`)."
}
